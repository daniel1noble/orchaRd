language: R
r: TRAVIS_R_VERSION=4.2

sudo: required

warnings_are_errors: true

r_packages:
  - covr

after_success:
  - Rscript -e 'library(covr); codecov()'
  - bash <(curl -s https://codecov.io/bash)

